# Copy this file to ".env" and fill in your real values.
# Get these from the TestRelic platform: Settings -> API Keys (and your repo id).
# NEVER commit your real .env — it's already in .gitignore.
TESTRELIC_API_KEY=your_api_key_here
TESTRELIC_REPO_ID=your_repo_id_here
